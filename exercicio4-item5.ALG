Algoritmo "tabuada"

Var
   base:inteiro
   usr:real

Inicio

   escreva("escolha um numero para ver a tabuada: ")
   leia(usr)
   para base de 0 ate 10 faca
      escreval (base," x",usr," = ",usr*base)
   fimpara

Fimalgoritmo